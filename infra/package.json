{
  "name": "verily-infra",
  "version": "0.1.0",
  "private": true,
  "description": "Pulumi infrastructure for Verily — AWS and GCP deployments",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "preview": "pulumi preview",
    "up": "pulumi up",
    "destroy": "pulumi destroy"
  },
  "dependencies": {
    "@pulumi/aws": "^6.0.0",
    "@pulumi/awsx": "^2.0.0",
    "@pulumi/docker-build": "^0.0.8",
    "@pulumi/gcp": "^8.0.0",
    "@pulumi/pulumi": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.8.0"
  }
}
